<UserControl x:Class="SchoolAppCore.Views.LoginPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SchoolAppCore.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Username" Margin="10 10 10 5"/>
        <TextBox Grid.Row="1" x:Name="txtUsername" Margin="10 10 10 5"/>

        <TextBlock Grid.Row="2" Text="Password" Margin="10 10 10 5"/>
        <PasswordBox Grid.Row="3" x:Name="txtPassword" Margin="10 10 10 5"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Exit" Margin="10" Click="Button_Click"/>
            <Button Content="Login" Margin="10" Command="{Binding LoginCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
